# --- core (always needed, lightweight, runs offline) ---
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.111
uvicorn[standard]>=0.30
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
prometheus-client>=0.20

# --- live mode (real LLMs + ADK + tracing). Optional for local dev/CI. ---
# google-adk>=2.0            # Google Agent Development Kit (Sequential/Parallel/Loop agents)
# google-genai>=1.0          # direct Gemini completions
# litellm>=1.40              # OpenAI (and others) via ADK's LiteLlm wrapper
# openai>=1.30               # direct OpenAI completions
# google-cloud-bigquery>=3.0 # real BigQuery data layer
# langfuse>=3.0              # Langfuse client
# openinference-instrumentation-google-adk>=0.1.10  # auto-span every ADK node

# --- dev ---
pytest>=8.0
httpx>=0.27                  # FastAPI TestClient
litellm>=1.40
python-dotenv>=1.0
